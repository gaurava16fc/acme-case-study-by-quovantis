import { CustomerSavingAccountsResolver } from './customer-saving-accounts.resolver';

describe('CustomerSavingAccountsResolver', () => {
  it('should create an instance', () => {
    expect(new CustomerSavingAccountsResolver()).toBeTruthy();
  });
});
